---
- hosts: all
  gather_facts: yes
  become: true
  become_user: ibrahim
  become_method: sudo
  vars:
    mongodb_password: "{{  morpheus['customOptions']['mongodb_password'] }}"
  tasks:         
    - name: MongoDB Deployment
      include_role:
        name: mongo-3_0
